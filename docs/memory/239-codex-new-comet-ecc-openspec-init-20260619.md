# 239-codex-new comet-ecc OpenSpec init

Recorded: 2026-06-19 China time
Owner/prefix: `239-codex-new-`

## Fact

`onespec` command was not present. `openspec` was present and used.

Project:

`G:\codex-project\comet-ecc`

Command:

`openspec init --tools codex`

## Result

OpenSpec/Codex skills were created under `.codex/skills`.

OpenSpec directories were preserved with `.gitkeep` files under:

1. `openspec/changes`
2. `openspec/changes/archive`
3. `openspec/specs`

Local commit:

`4e5c57d chore: initialize openspec for codex`

No remote is configured for `G:\codex-project\comet-ecc`, so there was no push.
